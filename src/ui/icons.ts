import {
  createIcons,
  Download,
  Plus,
  ArrowLeft,
  Undo,
  Redo,
  Settings,
  X,
  PenTool,
  Pencil,
  Lasso,
  Link,
  Unlink,
  Hand,
  Maximize,
  Palette,
  Trash2,
  FileJson,
  FileCode,
  Image as ImageIcon,
  ArrowUpToLine,
  ArrowDownToLine,
} from "lucide";

export function refreshIcons() {
  createIcons({
    icons: {
      Download,
      Plus,
      ArrowLeft,
      Undo,
      Redo,
      Pencil,
      Settings,
      X,
      PenTool,
      Lasso,
      Link,
      Unlink,
      Hand,
      Maximize,
      Palette,
      Trash2,
      FileJson,
      FileCode,
      Image: ImageIcon,
      ArrowUpToLine,
      ArrowDownToLine,
    },
  });
}
